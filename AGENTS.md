## Before push
`npm run build && npm run test` — fix failures first.

## Skills
Use `skill` to load. Available:

| Skill | Use |
|-------|-----|
| `build-test-push` | Build → test → commit → push |
| `fix-production` | Fix Railway deploy errors |
| `add-api-endpoint` | New controller + route pattern |
| `add-db-schema` | New tables/columns/indexes pattern |
